import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-inter',
});

const playfair = Playfair_Display({ 
  subsets: ["latin"],
  variable: '--font-playfair',
});

export const metadata: Metadata = {
  title: "Eskeen Clinic | Advanced Aesthetics by Nurse Elanda",
  description: "Experience premium aesthetic treatments with Nurse Elanda. Specializing in dermal fillers, skin boosters, and NAD+ therapy in London.",
  keywords: "aesthetic clinic, dermal fillers, skin boosters, NAD+ therapy, Nurse Elanda, London aesthetics",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} ${playfair.variable}`}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}